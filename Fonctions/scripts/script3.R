# Laissez-moi vous montrer un exemple d'une fonction que je vais créer et qui 
# s'appelle increment(). La plupart du temps, je veux utiliser cette fonction pour 
# augmenter la valeur d'un nombre de un. Cette fonction prend deux arguments : 
# "nombre" et "par" où "nombre" est le chiffre que je veux incrémenter et "par" est 
# la quantité dont je veux incrémenter "nombre". J'ai écrit la fonction ci-dessous. 
#
# increment <- function(nombreber, par = 1){
#     nombre + par
# }
#
# Si vous regardez entre les parenthèses, vous pouvez voir que j'ai mis "par" égal 
# à 1. Cela signifie que l'argument "par" aura la valeur par défaut de 1.
#
# Je peux maintenant utiliser la fonction d'incrémentation sans fournir une valeur 
# pour "par" : increment(5) sera évalué à 6. 
#
# Cependant, si je veux fournir une valeur pour l'argument "par", je peux toujours 
# le faire ! L'expression : increment(5, 2) sera évaluée à 7. 
# 
# Vous allez écrire une fonction appelée "solde". solde() prendra deux arguments : 
# "nombre" et "diviseur" où "nombre" est divisé par "diviseur" et le solde est 
# retourné. Imaginez que vous voulez généralement connaître le solde lorsque vous 
# divisez par 2, alors définissez la valeur par défaut de "diviseur" à 2. Assurez-vous 
# que "nombre" est le premier argument et "diviseur" le second.
#
# Indice n° 1 : vous pouvez utiliser l'opérateur de modulation %% pour trouver le solde.
#   Exemple : 7 %% 4 est égal à 3. 
#
# N'oubliez pas de définir les valeurs par défaut appropriées ! Veillez à enregistrer ce 
# script et à taper submit() dans la console après avoir écrit la fonction.

solde <- function(nombre, diviseur) {
  # Écrivez votre code ici !
  # N'oubliez pas : la dernière expression évaluée sera retournée ! 
}
